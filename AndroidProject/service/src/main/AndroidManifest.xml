<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidProject">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".services.FirstSercie"
            android:description="test1"
            android:enabled="true"
            android:exported="false"
            />
<!--        <service android:name="연결된 서비스 코드 위치(MainActivity처럼)"-->
<!--            android:description="사용자에게 서비스를 설명"-->
<!--            android:directBootAware=["true" | "false"]-->
<!--        // 사용자의 기기가 잠금을 해제하지 않고도 실행할 여부 default value=false-->
<!--        android:enabled=["true" | "false"]-->
<!--        // 서비스를 인스턴스화 될 수 있는지 여부-->
<!--        android:exported=["true" | "false"]-->
<!--        // 다른 어플리케이션의 컴포넌트가 서비스를 호출하거나 상호작용할 수 있는지에 대한 여부-->
<!--        android:foregroundServiceType=["camera" | "connectedDevice" | "dataSync" | "location" | "mediaPlaybak" | "mediaProjection" | "microphone" | "phoneCall"]	// 서비스가 특정 사용 사례를 충족하는 foregrond 서비스로 지정-->
<!--        android:icon="drawable resource"-->
<!--        // 서비스 아이콘 default 어플리케이션-->
<!--        android:isolatedProcess=["true" | "false"]-->
<!--        // true 설정 시 시스템에서 분리된 process 아래 실행되어 자체적인 권한이 없다.-->
<!--        android:label="string resource"-->
<!--        // 서비스 자체 이름-->
<!--        android:permission="string"-->
<!--        // binding하기 위해 entity가 가져야 하는 권한-->
<!--        android:process="string"-->
<!--        // 실행될 process 이름-->
<!--        >-->
<!--        . . .-->
<!--    </service>-->
    </application>

</manifest>